using UnityEngine;

public class GameManager : MonoBehaviour
{
    public static int collectedCoins = 0;

    // Optional: You can add additional methods or variables in the GameManager script to manage the game state, score, etc.
    // For simplicity, we will only focus on the collectedCoins variable in this example.
}
